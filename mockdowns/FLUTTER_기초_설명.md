# 🎓 Flutter 기초 개념 - 현재 프로젝트로 배우기

## 📱 Flutter란?

Flutter는 Google에서 만든 **크로스 플랫폼 앱 개발 프레임워크**입니다.
- 하나의 코드로 Android, iOS, Web, Windows 등 여러 플랫폼에서 실행 가능
- **위젯(Widget)** 기반으로 모든 것을 구성
- **Dart** 언어 사용

---

## 🏗️ 현재 프로젝트 구조 이해하기

### 1️⃣ **main.dart 파일 구조**

```dart
// 1. import: 필요한 패키지 가져오기
import 'package:flutter/material.dart';

// 2. main 함수: 앱의 시작점 (모든 Dart 프로그램의 진입점)
void main() {
  runApp(const MyApp());  // MyApp 위젯을 실행
}
```

**핵심 개념:**
- `main()`: 프로그램이 시작되는 곳
- `runApp()`: Flutter 앱을 실행하는 함수
- `const`: 컴파일 타임에 값이 정해지는 상수 (성능 최적화)

---

### 2️⃣ **StatelessWidget vs StatefulWidget**

#### **StatelessWidget** (상태가 없는 위젯)
```dart
class MyApp extends StatelessWidget {
  // 한 번 만들어진 후 변경되지 않는 위젯
  // 예: 앱 설정, 테마 등
}
```

**특징:**
- 한 번 생성되면 변경되지 않음
- `build()` 메서드만 있음
- 빠르고 가벼움

#### **StatefulWidget** (상태가 있는 위젯)
```dart
class MyHomePage extends StatefulWidget {
  // 상태(데이터)가 변경될 수 있는 위젯
  // 예: 카운터, 사용자 입력 등
}
```

**특징:**
- 데이터가 변경될 수 있음
- `State` 클래스와 함께 사용
- `setState()`로 UI 업데이트

---

### 3️⃣ **현재 프로젝트 코드 상세 분석**

#### **MyApp 클래스** (앱 전체 설정)

```dart
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(  // Material Design 스타일의 앱
      title: 'Chapter 1 - Flutter 학습',
      theme: ThemeData(  // 앱의 테마(색상, 폰트 등) 설정
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
        useMaterial3: true,  // Material Design 3 사용
      ),
      home: const MyHomePage(...),  // 앱이 시작될 때 보여줄 화면
    );
  }
}
```

**핵심 위젯:**
- `MaterialApp`: Material Design 앱의 루트 위젯
- `ThemeData`: 앱 전체의 색상, 폰트 등 스타일 설정
- `home`: 앱의 첫 화면

---

#### **MyHomePage 클래스** (메인 화면)

```dart
class MyHomePage extends StatefulWidget {
  final String title;  // 위젯에 전달받는 데이터 (변경 불가)
  
  // State 객체를 생성하는 메서드
  @override
  State<MyHomePage> createState() => _MyHomePageState();
}
```

**핵심 개념:**
- `final`: 한 번 할당되면 변경 불가
- `StatefulWidget`은 `State` 클래스와 쌍으로 작동

---

#### **_MyHomePageState 클래스** (실제 상태 관리)

```dart
class _MyHomePageState extends State<MyHomePage> {
  int _counter = 0;  // 상태 변수 (언더스코어는 private 의미)
  
  // 카운터를 증가시키는 함수
  void _incrementCounter() {
    setState(() {  // 상태 변경을 알림 → UI 다시 그리기
      _counter++;
    });
  }
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(  // 기본 화면 구조 제공
      appBar: AppBar(...),  // 상단 바
      body: Center(...),    // 본문
      floatingActionButton: FloatingActionButton(...),  // 플로팅 버튼
    );
  }
}
```

**핵심 개념:**
- `_counter`: 변경 가능한 상태 변수
- `setState()`: 상태 변경 후 UI를 다시 그리도록 알림
- `Scaffold`: Material Design의 기본 화면 레이아웃

---

### 4️⃣ **위젯 트리 구조**

현재 프로젝트의 위젯 계층 구조:

```
MaterialApp (앱 전체)
  └── MyHomePage (메인 화면)
      └── Scaffold (기본 레이아웃)
          ├── AppBar (상단 바)
          ├── Body (본문)
          │   └── Center (중앙 정렬)
          │       └── Column (세로 배치)
          │           ├── Text (텍스트 1)
          │           └── Text (카운터 숫자)
          └── FloatingActionButton (플로팅 버튼)
```

**핵심 위젯 설명:**
- `Scaffold`: 화면의 기본 골격 (앱바, 본문, 버튼 영역)
- `AppBar`: 상단 앱 바
- `Center`: 자식을 중앙에 배치
- `Column`: 자식들을 세로로 배치
- `Text`: 텍스트 표시
- `FloatingActionButton`: 둥근 플로팅 버튼

---

### 5️⃣ **상태 관리의 핵심: setState()**

```dart
void _incrementCounter() {
  setState(() {  // 이 함수 안에서 상태를 변경
    _counter++;  // 카운터 증가
  });
  // setState()가 호출되면 build() 메서드가 다시 실행됨
  // → 화면이 업데이트됨
}
```

**동작 원리:**
1. 버튼 클릭 → `_incrementCounter()` 호출
2. `setState()` 내부에서 `_counter` 증가
3. Flutter가 변경 감지
4. `build()` 메서드 자동 재실행
5. 화면에 새로운 값 표시

---

### 6️⃣ **pubspec.yaml 이해하기**

```yaml
name: chapter_1  # 프로젝트 이름
version: 1.0.0+1  # 버전 번호

dependencies:  # 앱 실행에 필요한 패키지
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2  # iOS 스타일 아이콘

dev_dependencies:  # 개발 시에만 필요한 패키지
  flutter_test: ...
  flutter_lints: ^2.0.0  # 코드 품질 검사 도구
```

**핵심 개념:**
- `dependencies`: 앱 실행에 필수
- `dev_dependencies`: 개발/테스트 시에만 사용
- `^1.0.2`: 버전 범위 (1.0.2 이상, 2.0.0 미만)

---

## 🎯 핵심 정리

### Flutter의 3가지 핵심 원칙

1. **Everything is a Widget** (모든 것이 위젯)
   - 화면, 버튼, 텍스트 모두 위젯
   - 위젯을 조합해서 앱 구성

2. **Widget Tree** (위젯 트리)
   - 위젯들이 부모-자식 관계로 트리 구조 형성
   - 부모 위젯이 자식 위젯을 포함

3. **State Management** (상태 관리)
   - `StatefulWidget`으로 변경 가능한 UI 생성
   - `setState()`로 상태 변경 후 UI 업데이트

---

## 🚀 다음 단계 학습 추천

1. **위젯 더 알아보기**
   - `Row` (가로 배치)
   - `Container` (컨테이너)
   - `ListView` (리스트)

2. **상태 관리 심화**
   - Provider 패키지
   - Riverpod 패키지
   - Bloc 패턴

3. **네비게이션**
   - 화면 간 이동
   - 라우팅

4. **API 연동**
   - HTTP 요청
   - JSON 파싱

---

## 💡 실습 과제

현재 프로젝트를 수정해보세요:

1. **카운터 감소 버튼 추가**
2. **카운터 리셋 버튼 추가**
3. **색상 변경하기**
4. **텍스트 스타일 변경하기**
