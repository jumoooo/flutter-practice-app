# 📦 6단계: 앱 배포 준비

## 🎯 학습 목표
- 앱 아이콘 설정하기
- 앱 이름 및 버전 관리하기
- 빌드 설정하기
- 테스트 및 디버깅하기
- 성능 최적화하기

---

## 📚 핵심 개념

### 1. 앱 아이콘 설정

#### flutter_launcher_icons 패키지 사용

```yaml
# pubspec.yaml
dev_dependencies:
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/icon.png"
```

```bash
flutter pub get
flutter pub run flutter_launcher_icons
```

---

### 2. 앱 이름 변경

#### Android
`android/app/src/main/AndroidManifest.xml`
```xml
<application
    android:label="내 앱 이름"
    ...>
</application>
```

#### iOS
`ios/Runner/Info.plist`
```xml
<key>CFBundleName</key>
<string>내 앱 이름</string>
```

---

### 3. 버전 관리

`pubspec.yaml`
```yaml
version: 1.0.0+1
# 형식: 버전명+빌드번호
# 버전명: 사용자에게 보이는 버전
# 빌드번호: 앱스토어에 업로드할 때 사용
```

---

### 4. Android 빌드

#### APK 빌드
```bash
flutter build apk --release
```

#### App Bundle 빌드 (권장)
```bash
flutter build appbundle --release
```

---

### 5. iOS 빌드

```bash
flutter build ios --release
```

---

## 💡 다음 단계

이제 앱 배포 준비를 배웠습니다!

**다음 단계:** `실습_문제.md`를 열어서 직접 해보세요!

**추가 학습:**
- `07_GitHub_배포_이력서/` - GitHub 배포 및 이력서 작성

---

## 📚 참고 자료

- [Flutter 배포 가이드](https://docs.flutter.dev/deployment)
- [Android 배포](https://docs.flutter.dev/deployment/android)
- [iOS 배포](https://docs.flutter.dev/deployment/ios)

**준비되셨나요? 실습 문제로 넘어가세요! 🚀**
