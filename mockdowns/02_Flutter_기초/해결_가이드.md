# 💡 2단계 해결 가이드: Flutter 기초

## 문제 해결 방법 모음

이 문서는 실습 문제를 풀다가 막혔을 때 참고하는 가이드입니다.

---

## 문제 1: 기본 위젯 사용하기

### ❌ "Text 위젯이 제대로 표시되지 않음"

**해결 방법:**
```dart
// 올바른 사용법
Text(
  '안녕하세요',
  style: TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
    color: Colors.blue,
  ),
)
```

**주의사항:**
- `style` 속성은 `TextStyle` 객체를 받습니다
- `fontSize`는 `double` 타입입니다 (따옴표 없이)

---

### ❌ "Container가 화면에 보이지 않음"

**해결 방법:**
```dart
// 색상이나 테두리가 있어야 보입니다
Container(
  width: 200,
  height: 100,
  decoration: BoxDecoration(
    color: Colors.blue,  // 배경색 필수!
    borderRadius: BorderRadius.circular(10),
  ),
  child: Center(child: Text('내용')),
)
```

**주의사항:**
- `decoration`과 `color` 속성을 동시에 사용할 수 없습니다
- `decoration`의 `BoxDecoration` 안에 `color`를 넣어야 합니다

---

### ❌ "Row/Column에서 위젯이 겹침"

**해결 방법:**
```dart
// children 리스트 사용
Row(
  children: [
    Text('첫 번째'),
    Text('두 번째'),
    Text('세 번째'),
  ],
)

// 또는 Expanded로 공간 분배
Row(
  children: [
    Expanded(child: Text('첫 번째')),
    Expanded(child: Text('두 번째')),
  ],
)
```

---

## 문제 2: 카운터 앱 개선하기

### ❌ "감소 버튼이 작동하지 않음"

**해결 방법:**
```dart
// 감소 함수 추가
void _decrementCounter() {
  setState(() {
    if (_counter > 0) {  // 0 이하로 내려가지 않도록
      _counter--;
    }
  });
}

// 버튼에 연결
FloatingActionButton(
  onPressed: _decrementCounter,
  child: Icon(Icons.remove),
)
```

---

### ❌ "여러 버튼을 한 줄에 배치하고 싶음"

**해결 방법:**
```dart
// Row로 감싸기
floatingActionButton: Row(
  mainAxisAlignment: MainAxisAlignment.end,
  children: [
    FloatingActionButton(
      onPressed: _decrementCounter,
      child: Icon(Icons.remove),
    ),
    SizedBox(width: 16),  // 간격
    FloatingActionButton(
      onPressed: _incrementCounter,
      child: Icon(Icons.add),
    ),
  ],
)
```

---

### ❌ "조건부 색상이 작동하지 않음"

**해결 방법:**
```dart
// 삼항 연산자 사용
Text(
  '$_counter',
  style: TextStyle(
    fontSize: 48,
    color: _counter >= 10 ? Colors.red : Colors.blue,
    fontWeight: FontWeight.bold,
  ),
)

// 또는 변수로 분리
Color get counterColor {
  return _counter >= 10 ? Colors.red : Colors.blue;
}
```

---

## 문제 3: 프로필 카드 만들기

### ❌ "그림자가 보이지 않음"

**해결 방법:**
```dart
Container(
  decoration: BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.circular(15),
    boxShadow: [
      BoxShadow(
        color: Colors.grey.withOpacity(0.3),  // 투명도 중요!
        spreadRadius: 2,
        blurRadius: 5,
        offset: Offset(0, 3),  // 그림자 위치
      ),
    ],
  ),
)
```

**주의사항:**
- `withOpacity()`로 투명도를 조절해야 그림자가 보입니다
- `offset`으로 그림자 위치를 조절할 수 있습니다

---

### ❌ "CircleAvatar에 이미지가 표시되지 않음"

**해결 방법:**
```dart
// 네트워크 이미지
CircleAvatar(
  radius: 50,
  backgroundImage: NetworkImage('https://example.com/image.jpg'),
)

// 또는 아이콘 사용
CircleAvatar(
  radius: 50,
  child: Icon(Icons.person, size: 50),
)

// 또는 로컬 이미지
CircleAvatar(
  radius: 50,
  backgroundImage: AssetImage('assets/profile.jpg'),
)
```

---

## 문제 4: 할 일 리스트 만들기

### ❌ "TextField가 작동하지 않음"

**해결 방법:**
```dart
// TextEditingController 필요
class _TodoListState extends State<TodoList> {
  TextEditingController controller = TextEditingController();
  
  @override
  void dispose() {
    controller.dispose();  // 메모리 누수 방지
    super.dispose();
  }
  
  @override
  Widget build(BuildContext context) {
    return TextField(
      controller: controller,
      decoration: InputDecoration(
        hintText: '할 일을 입력하세요',
      ),
    );
  }
}
```

---

### ❌ "ListView가 스크롤되지 않음"

**해결 방법:**
```dart
// Expanded로 감싸기
Column(
  children: [
    // 입력 필드
    TextField(...),
    // ListView는 Expanded로 감싸야 함
    Expanded(
      child: ListView.builder(
        itemCount: todos.length,
        itemBuilder: (context, index) {
          return ListTile(...);
        },
      ),
    ),
  ],
)
```

**주의사항:**
- Column 안에 ListView를 직접 넣으면 에러가 발생합니다
- Expanded나 SizedBox로 높이를 지정해야 합니다

---

### ❌ "항목 삭제 시 에러 발생"

**해결 방법:**
```dart
// 안전하게 삭제
void removeTodo(int index) {
  setState(() {
    if (index >= 0 && index < todos.length) {
      todos.removeAt(index);
    }
  });
}

// 또는
void removeTodo(int index) {
  if (index < 0 || index >= todos.length) return;
  
  setState(() {
    todos.removeAt(index);
  });
}
```

---

## 문제 5: 반응형 레이아웃 만들기

### ❌ "화면 회전이 감지되지 않음"

**해결 방법:**
```dart
@override
Widget build(BuildContext context) {
  // MediaQuery로 화면 정보 가져오기
  final size = MediaQuery.of(context).size;
  final orientation = MediaQuery.of(context).orientation;
  
  // 또는
  final isPortrait = size.height > size.width;
  
  if (orientation == Orientation.portrait) {
    return Column(...);
  } else {
    return Row(...);
  }
}
```

---

### ❌ "가로 모드에서 레이아웃이 깨짐"

**해결 방법:**
```dart
// Flexible이나 Expanded 사용
Row(
  children: [
    Expanded(
      child: Container(...),  // 공간을 나눠 가짐
    ),
    Expanded(
      child: Container(...),
    ),
  ],
)
```

---

## 문제 6: 테마 커스터마이징

### ❌ "테마가 적용되지 않음"

**해결 방법:**
```dart
// MaterialApp의 theme 속성 사용
MaterialApp(
  theme: ThemeData(
    colorScheme: ColorScheme.fromSeed(
      seedColor: Colors.purple,
      brightness: Brightness.light,
    ),
    useMaterial3: true,
  ),
  // ...
)

// 위젯에서 테마 사용
Text(
  '제목',
  style: Theme.of(context).textTheme.headlineLarge,
)
```

---

### ❌ "커스텀 색상이 적용되지 않음"

**해결 방법:**
```dart
// ColorScheme 직접 정의
colorScheme: ColorScheme(
  primary: Colors.purple,
  secondary: Colors.orange,
  surface: Colors.white,
  error: Colors.red,
  onPrimary: Colors.white,
  onSecondary: Colors.white,
  brightness: Brightness.light,
)
```

---

## 💡 일반적인 실수들

### 1. setState()를 빼먹음
```dart
// ❌ 잘못된 방법
void _incrementCounter() {
  _counter++;  // UI가 업데이트되지 않음!
}

// ✅ 올바른 방법
void _incrementCounter() {
  setState(() {
    _counter++;  // UI가 업데이트됨
  });
}
```

---

### 2. const를 잘못 사용
```dart
// ❌ 잘못된 방법
const Text('$_counter')  // 변수가 있으면 const 사용 불가

// ✅ 올바른 방법
Text('$_counter')  // const 제거
```

---

### 3. children 리스트를 빼먹음
```dart
// ❌ 잘못된 방법
Column(
  Text('첫 번째'),
  Text('두 번째'),
)

// ✅ 올바른 방법
Column(
  children: [
    Text('첫 번째'),
    Text('두 번째'),
  ],
)
```

---

## 🆘 디버깅 팁

### 1. Hot Reload가 작동하지 않을 때
- 완전히 재시작: `R` (대문자) 키
- 또는 앱 종료 후 `flutter run` 다시 실행

### 2. 에러 메시지 읽기
- 에러 메시지의 첫 줄이 가장 중요합니다
- "The method 'xxx' isn't defined" → 메서드 이름 확인
- "Undefined name 'xxx'" → 변수 선언 확인

### 3. 화면에 아무것도 안 보일 때
- Scaffold로 감싸기
- MaterialApp이 있는지 확인
- build() 메서드가 제대로 return하는지 확인

---

## ✅ 체크리스트

문제를 해결했는지 확인하세요:

- [ ] 모든 위젯이 정상적으로 표시됨
- [ ] 상태 변경이 UI에 반영됨
- [ ] 에러 없이 앱이 실행됨
- [ ] 핫 리로드가 정상 작동함

---

**다음 단계로 넘어갈 준비가 되셨나요?** 🚀
