# 🎨 2단계: Flutter 기초 - 위젯과 레이아웃

## 🎯 학습 목표
- 위젯의 개념과 종류 이해하기
- 기본 위젯들 사용법 익히기
- 레이아웃 위젯으로 화면 구성하기
- 상태 관리 기초 (setState) 이해하기
- 스타일링 방법 배우기

---

## 📚 핵심 개념

### 1. Everything is a Widget (모든 것이 위젯)

Flutter에서 화면의 모든 요소는 **위젯(Widget)**입니다.
- 텍스트 → `Text` 위젯
- 버튼 → `ElevatedButton` 위젯
- 이미지 → `Image` 위젯
- 화면 전체 → `Scaffold` 위젯

**위젯을 조합해서 앱을 만듭니다!**

---

### 2. StatelessWidget vs StatefulWidget

#### StatelessWidget (상태가 없는 위젯)
```dart
class MyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text('안녕하세요');
  }
}
```

**특징:**
- 한 번 생성되면 변경되지 않음
- 정적인 UI에 사용
- 예: 앱바, 아이콘, 정적 텍스트

#### StatefulWidget (상태가 있는 위젯)
```dart
class CounterWidget extends StatefulWidget {
  @override
  State<CounterWidget> createState() => _CounterWidgetState();
}

class _CounterWidgetState extends State<CounterWidget> {
  int count = 0;  // 상태 변수
  
  void increment() {
    setState(() {  // 상태 변경 알림
      count++;
    });
  }
  
  @override
  Widget build(BuildContext context) {
    return Text('$count');
  }
}
```

**특징:**
- 데이터가 변경될 수 있음
- 동적인 UI에 사용
- `setState()`로 UI 업데이트

---

## 🧩 기본 위젯들

### 1. Text (텍스트 표시)

```dart
Text(
  '안녕하세요',
  style: TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.bold,
    color: Colors.blue,
  ),
)
```

**주요 속성:**
- `style`: 텍스트 스타일 (색상, 크기, 폰트 등)
- `textAlign`: 정렬 방식

---

### 2. Container (컨테이너)

```dart
Container(
  width: 200,
  height: 100,
  padding: EdgeInsets.all(16),
  margin: EdgeInsets.all(8),
  decoration: BoxDecoration(
    color: Colors.blue,
    borderRadius: BorderRadius.circular(10),
  ),
  child: Text('내용'),
)
```

**주요 속성:**
- `width`, `height`: 크기
- `padding`: 내부 여백
- `margin`: 외부 여백
- `decoration`: 장식 (색상, 테두리, 그림자 등)
- `child`: 자식 위젯

---

### 3. Row (가로 배치)

```dart
Row(
  mainAxisAlignment: MainAxisAlignment.center,  // 주축 정렬
  crossAxisAlignment: CrossAxisAlignment.center,  // 교차축 정렬
  children: [
    Text('첫 번째'),
    Text('두 번째'),
    Text('세 번째'),
  ],
)
```

**주요 속성:**
- `mainAxisAlignment`: 주축(가로) 정렬
- `crossAxisAlignment`: 교차축(세로) 정렬
- `children`: 자식 위젯 리스트

---

### 4. Column (세로 배치)

```dart
Column(
  mainAxisAlignment: MainAxisAlignment.center,
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [
    Text('위'),
    Text('중간'),
    Text('아래'),
  ],
)
```

**Row와 동일한 속성, 방향만 다름**

---

### 5. ListView (스크롤 가능한 리스트)

```dart
ListView(
  children: [
    ListTile(title: Text('항목 1')),
    ListTile(title: Text('항목 2')),
    ListTile(title: Text('항목 3')),
  ],
)

// 또는 동적 리스트
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) {
    return ListTile(title: Text(items[index]));
  },
)
```

---

## 🏗️ 레이아웃 위젯들

### 1. Scaffold (기본 화면 구조)

```dart
Scaffold(
  appBar: AppBar(
    title: Text('제목'),
  ),
  body: Center(
    child: Text('본문'),
  ),
  floatingActionButton: FloatingActionButton(
    onPressed: () {},
    child: Icon(Icons.add),
  ),
)
```

**주요 속성:**
- `appBar`: 상단 앱 바
- `body`: 본문 영역
- `floatingActionButton`: 플로팅 버튼
- `drawer`: 사이드 메뉴
- `bottomNavigationBar`: 하단 네비게이션 바

---

### 2. AppBar (앱 바)

```dart
AppBar(
  title: Text('제목'),
  backgroundColor: Colors.blue,
  actions: [
    IconButton(icon: Icon(Icons.search), onPressed: () {}),
  ],
)
```

---

### 3. Center (중앙 정렬)

```dart
Center(
  child: Text('가운데'),
)
```

---

## 🎨 스타일링

### 1. 색상 (Colors)

```dart
Colors.blue           // 기본 색상
Colors.blue.shade100  // 밝은 톤
Colors.blue.shade900 // 어두운 톤
Color(0xFF2196F3)     // 16진수 색상 코드
```

---

### 2. 텍스트 스타일 (TextStyle)

```dart
TextStyle(
  fontSize: 24,
  fontWeight: FontWeight.bold,
  color: Colors.blue,
  fontFamily: 'Roboto',
  letterSpacing: 1.2,
)
```

---

### 3. 테마 (Theme)

```dart
MaterialApp(
  theme: ThemeData(
    colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue),
    useMaterial3: true,
    textTheme: TextTheme(
      headlineLarge: TextStyle(fontSize: 32),
    ),
  ),
)
```

---

## 🔄 상태 관리 기초: setState()

### setState()의 역할

```dart
void _incrementCounter() {
  setState(() {
    _counter++;  // 상태 변경
  });
  // setState() 호출 후 build() 메서드가 자동으로 다시 실행됨
  // → 화면이 업데이트됨
}
```

**동작 원리:**
1. `setState()` 내부에서 상태 변수 변경
2. Flutter가 변경 감지
3. `build()` 메서드 자동 재실행
4. 화면 업데이트

---

## 📱 실전 예제: 간단한 프로필 카드

```dart
class ProfileCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(10),
        boxShadow: [
          BoxShadow(
            color: Colors.grey.withOpacity(0.3),
            spreadRadius: 2,
            blurRadius: 5,
          ),
        ],
      ),
      child: Column(
        children: [
          CircleAvatar(
            radius: 50,
            backgroundImage: NetworkImage('이미지_URL'),
          ),
          SizedBox(height: 16),
          Text(
            '홍길동',
            style: TextStyle(
              fontSize: 24,
              fontWeight: FontWeight.bold,
            ),
          ),
          SizedBox(height: 8),
          Text('Flutter 개발자'),
        ],
      ),
    );
  }
}
```

---

## 🎯 핵심 정리

### 위젯 트리 구조
```
Scaffold
  ├── AppBar
  └── Body
      └── Column
          ├── Text
          └── Container
              └── Text
```

### 상태 관리 흐름
1. 사용자 액션 (버튼 클릭 등)
2. 이벤트 핸들러 호출
3. `setState()` 내부에서 상태 변경
4. `build()` 메서드 재실행
5. 화면 업데이트

---

## 💡 다음 단계

이제 기본 위젯들을 이해했습니다!

**다음 단계:** `실습_문제.md`를 열어서 직접 코드를 작성해보세요!

**추가 학습:**
- `03_네비게이션_라우팅/` - 화면 간 이동 배우기
- `04_API_연동/` - 데이터 가져오기

---

## 📚 참고 자료

- [Flutter 위젯 카탈로그](https://docs.flutter.dev/ui/widgets)
- [Material Design 3](https://m3.material.io/)
- [Flutter 레이아웃 튜토리얼](https://docs.flutter.dev/ui/layout)

**준비되셨나요? 실습 문제로 넘어가세요! 🚀**
