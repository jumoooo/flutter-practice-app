# 🔄 5단계: 상태 관리 심화

## 🎯 학습 목표
- Provider 패키지 사용하기
- 전역 상태 관리 이해하기
- 상태 관리 패턴 학습하기
- 실제 프로젝트에 적용하기

---

## 📚 핵심 개념

### 1. Provider 패키지

#### 패키지 추가
```yaml
# pubspec.yaml
dependencies:
  provider: ^6.1.1
```

```dart
import 'package:provider/provider.dart';
```

---

### 2. Provider 설정

```dart
// main.dart
void main() {
  runApp(
    ChangeNotifierProvider(
      create: (context) => CounterProvider(),
      child: MyApp(),
    ),
  );
}
```

---

### 3. Provider 모델 만들기

```dart
// counter_provider.dart
import 'package:flutter/foundation.dart';

class CounterProvider with ChangeNotifier {
  int _count = 0;
  
  int get count => _count;
  
  void increment() {
    _count++;
    notifyListeners();  // 변경 사항 알림
  }
  
  void decrement() {
    _count--;
    notifyListeners();
  }
}
```

---

### 4. Provider 사용하기

```dart
// Consumer 사용
Consumer<CounterProvider>(
  builder: (context, counter, child) {
    return Text('${counter.count}');
  },
)

// 또는 Provider.of 사용
final counter = Provider.of<CounterProvider>(context);
Text('${counter.count}')

// 또는 context.watch 사용 (권장)
final counter = context.watch<CounterProvider>();
Text('${counter.count}')
```

---

### 5. 여러 Provider 사용

```dart
MultiProvider(
  providers: [
    ChangeNotifierProvider(create: (_) => CounterProvider()),
    ChangeNotifierProvider(create: (_) => UserProvider()),
    ChangeNotifierProvider(create: (_) => ThemeProvider()),
  ],
  child: MyApp(),
)
```

---

## 🎯 실전 예제

### 예제 1: 사용자 정보 관리

```dart
// user_provider.dart
class UserProvider with ChangeNotifier {
  User? _user;
  bool _isLoading = false;
  
  User? get user => _user;
  bool get isLoading => _isLoading;
  
  Future<void> loadUser() async {
    _isLoading = true;
    notifyListeners();
    
    try {
      _user = await ApiService.fetchUser();
    } catch (e) {
      print('에러: $e');
    } finally {
      _isLoading = false;
      notifyListeners();
    }
  }
  
  void logout() {
    _user = null;
    notifyListeners();
  }
}
```

### 예제 2: 테마 관리

```dart
// theme_provider.dart
class ThemeProvider with ChangeNotifier {
  bool _isDarkMode = false;
  
  bool get isDarkMode => _isDarkMode;
  ThemeData get theme => _isDarkMode ? ThemeData.dark() : ThemeData.light();
  
  void toggleTheme() {
    _isDarkMode = !_isDarkMode;
    notifyListeners();
  }
}

// 사용
MaterialApp(
  theme: context.watch<ThemeProvider>().theme,
)
```

---

## 💡 다음 단계

이제 상태 관리를 배웠습니다!

**다음 단계:** `실습_문제.md`를 열어서 직접 구현해보세요!

**추가 학습:**
- `06_앱_배포_준비/` - 앱 배포 준비하기

---

## 📚 참고 자료

- [Provider 패키지](https://pub.dev/packages/provider)
- [Flutter 상태 관리](https://docs.flutter.dev/data-and-backend/state-mgmt)

**준비되셨나요? 실습 문제로 넘어가세요! 🚀**
